/** 環境変数 (本番用) */
export const environment = {
  /** 本番環境モード */
  production: true,
  /** API サーバの URL・末尾スラッシュなし・自分のサーバで動かす場合は localhost 部分を書き換える (本番ビルド時は未使用) */
  apiUrl: 'http://localhost:2323/api'
};
